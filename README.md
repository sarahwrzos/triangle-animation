# Triangle Animation

This project uses **Three.js** to animate a triangle folding into a **tetrahedron**.

## Getting Started

To run the animation locally:

1. Open a terminal in the project directory.
2. Start a simple HTTP server using Python:

   ```bash
   python -m http.server
